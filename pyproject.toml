[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "jev-evolve"
version = "0.1.0"
description = "Self-evolving agents whose every decision is a typed question, with an honest report of how much of the improvement is selection noise"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "novaleolin" }]
keywords = [
  "agent", "llm", "jev", "typesafe", "system-one", "typed-decisions",
  "self-improving", "self-evolving", "prompt-optimization", "evaluation",
  "selection-bias", "agent-framework",
]
# The license is declared as a PEP 639 expression above; setuptools rejects
# a license trove classifier alongside it. The Python version classifiers stay:
# shields.io reads pypi/pyversions from them, not from requires-python.
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = ["evalfloor>=0.3.0"]

[project.optional-dependencies]
local = ["torch", "transformers"]
dev = ["pytest"]

[project.urls]
Homepage = "https://github.com/novaleolin/jev-evolve"
Source = "https://github.com/novaleolin/jev-evolve"
Issues = "https://github.com/novaleolin/jev-evolve/issues"

[tool.setuptools.packages.find]
include = ["jev_evolve*"]

[tool.setuptools.package-data]
jev_evolve = ["py.typed"]
